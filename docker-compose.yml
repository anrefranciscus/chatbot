version: "3.8"

services:

  rasa:
    build: .
    ports:
      - "5005:5005"
    volumes:
      - .:/app
    depends_on:
      - action_server

  action_server:
    build: ./actions
    ports:
      - "5055:5055"
    volumes:
      - ./actions:/app